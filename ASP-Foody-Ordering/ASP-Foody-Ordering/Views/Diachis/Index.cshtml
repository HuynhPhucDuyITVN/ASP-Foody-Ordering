@model IEnumerable<ASP_Foody_Ordering.Models.Diachi>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Danh sách địa chỉ</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Admin">Trang chủ</a></li>
        <li class="breadcrumb-item active">Địa chỉ</li>
    </ol>
    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-table me-1"></i>
            <p>
                <a class="btn btn-outline-info" asp-action="Create">Thêm mới</a>
            </p>
        </div>
        <div class="card-body">
            <table id="datatablesSimple">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.DiaChi)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PhuongXa)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.QuanHuyen)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.TinhThanh)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.MaTkNavigation)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.DiaChi)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PhuongXa)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.QuanHuyen)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.TinhThanh)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.MaTkNavigation)
                        </th>
                        <th></th>
                    </tr>
                </tfoot>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.DiaChi)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PhuongXa)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.QuanHuyen)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TinhThanh)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.MaTkNavigation.Ten)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.MaDc" class="btn btn-outline-warning">Cập nhật</a>
                                <a asp-action="Details" asp-route-id="@item.MaDc" class="btn btn-outline-info">Chi tiết</a>
                                <a asp-action="Delete" asp-route-id="@item.MaDc" class="btn btn-outline-danger">Xóa</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
